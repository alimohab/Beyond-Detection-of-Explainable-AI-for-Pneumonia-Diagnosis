# Minimal requirements for running the project (based on imports in src/)
# Versions are lower-bounded to ensure compatibility; adjust as needed.
torch>=2.0.0
torchvision>=0.15.0

# Numerical & scientific
numpy>=1.24.0
scipy>=1.11.0

# Video / image processing
opencv-python>=4.8.0
Pillow>=10.0.0

# ML / utilities
scikit-learn>=1.3.0
tqdm>=4.65.0
matplotlib>=3.7.0

# Optional (used for reports/visualizations if needed)
# pandas>=2.0.0
# seaborn>=0.12.0
# tensorboard>=2.13.0

